<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/high_contrast_background"
    android:padding="24dp">

    <!-- 主要内容区域 - 使用LinearLayout包装以保持垂直布局 -->
    <LinearLayout
        android:id="@+id/llMainContent"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:orientation="vertical">

        <!-- 标题 TextView -->
        <TextView
            android:id="@+id/tvTitle"
            style="@style/ElderlyFriendlyTitle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginBottom="32dp"
            android:contentDescription="@string/login_page_title_content_description"
            android:gravity="center"
            android:text="@string/login_page_title"
            android:textSize="24sp" />

        <!-- 用户名输入 -->
        <com.google.android.material.textfield.TextInputLayout
            android:id="@+id/tilUsername"
            style="@style/ElderlyFriendlyTextInputLayout"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginBottom="20dp"
            android:hint="@string/login_username_hint"
            app:helperText="@string/login_username_helper"
            app:helperTextTextAppearance="@style/ElderlyFriendlyText">

            <com.google.android.material.textfield.TextInputEditText
                android:id="@+id/etUsername"
                style="@style/ElderlyFriendlyEditText"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:contentDescription="@string/login_username_content_description"
                android:inputType="text"
                android:maxLength="20" />

        </com.google.android.material.textfield.TextInputLayout>

        <!-- 密码输入 -->
        <com.google.android.material.textfield.TextInputLayout
            android:id="@+id/tilPassword"
            style="@style/ElderlyFriendlyTextInputLayout"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginBottom="24dp"
            android:hint="@string/login_password_hint"
            app:helperText="@string/login_password_helper"
            app:helperTextTextAppearance="@style/ElderlyFriendlyText"
            app:passwordToggleContentDescription="@string/login_password_toggle_description"
            app:passwordToggleEnabled="true">

            <com.google.android.material.textfield.TextInputEditText
                android:id="@+id/etPassword"
                style="@style/ElderlyFriendlyEditText"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:contentDescription="@string/login_password_content_description"
                android:inputType="textPassword"
                android:maxLength="20" />

        </com.google.android.material.textfield.TextInputLayout>

        <!-- 记住我 CheckBox -->
        <CheckBox
            android:id="@+id/cbRememberMe"
            style="@style/ElderlyFriendlyCheckBox"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginBottom="24dp"
            android:contentDescription="@string/login_remember_me_content_description"
            android:text="@string/login_remember_me_text" />

        <!-- 登录按钮 -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnLogin"
            style="@style/ElderlyFriendlyButton"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginBottom="20dp"
            android:contentDescription="@string/login_button_content_description"
            android:minHeight="56dp"
            android:text="@string/login_button_text" />

        <!-- 注册链接 TextView -->
        <TextView
            android:id="@+id/tvRegisterLink"
            style="@style/ElderlyFriendlyLinkText"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:contentDescription="@string/login_register_link_content_description"
            android:gravity="center"
            android:padding="12dp"
            android:text="@string/login_register_link_text" />

    </LinearLayout>

    <!-- 消息提示 TextView - 固定在底部位置 -->
    <TextView
        android:id="@+id/tvMessage"
        style="@style/ElderlyFriendlyText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_marginBottom="24dp"
        android:background="@drawable/message_background"
                    android:contentDescription="@string/login_status_message_content_description"
        android:gravity="center"
        android:padding="12dp"
        android:visibility="gone" />

</RelativeLayout>